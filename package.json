{
  "name": "availio-booking-page",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd booking-app && npm install && npm run build",
    "vercel-build": "cd booking-app && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

