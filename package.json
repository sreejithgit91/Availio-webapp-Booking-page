{
  "name": "availio-booking-page",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd booking-app && npm install && npm run build",
    "dev": "cd booking-app && npm run dev",
    "start": "cd booking-app && npm run preview"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

